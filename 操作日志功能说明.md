# 操作日志功能说明

## 功能概述

本次更新实现了完整的操作日志记录功能,包括:

1. **操作人关联**: 每条记录都关联了操作人信息(createBy, updateBy)
2. **操作日志记录**: 使用AOP切面自动记录所有操作
3. **操作日志查询**: 提供多种查询方式,仅领导角色可查看

## 数据库变更

执行以下SQL脚本创建操作日志表:

```bash
mysql -u root -p accounting_db < src/main/resources/migration/V1.3__add_operation_log_table.sql
```

或直接执行SQL文件中的内容。

## 核心文件说明

### 1. 实体类
- `OperationLog.java`: 操作日志实体类

### 2. 注解和枚举
- `OperationLog.java`: 操作日志注解,用于标记需要记录日志的方法
- `OperationType.java`: 操作类型枚举(CREATE, UPDATE, DELETE, OTHER)
- `OperationModule.java`: 模块枚举(ASSET, LIABILITY, TRANSACTION, OTHER)

### 3. AOP切面
- `OperationLogAspect.java`: 自动记录操作日志的切面

### 4. Mapper
- `OperationLogMapper.java`: 操作日志数据访问层

### 5. Service
- `OperationLogService.java`: 操作日志服务层

### 6. Controller
- `OperationLogController.java`: 操作日志接口层(仅领导角色可访问)

## 使用方法

### 1. 在Service方法上添加注解

在需要记录操作日志的Service方法上添加`@OperationLog`注解:

```java
@OperationLog(value = "创建资产", operationType = OperationType.CREATE, module = OperationModule.ASSET)
public AssetDTO createAsset(AssetDTO assetDTO) {
    // 方法实现
}
```

### 2. 自动记录操作人信息

Service层已集成获取当前用户的功能,操作时自动设置`createBy`和`updateBy`字段。

### 3. 查询操作日志(仅领导角色)

以下接口仅领导角色(LEADER)可以访问:

#### 获取最近的操作日志
```http
GET /api/operation-logs/recent?limit=100
```

#### 根据ID获取操作日志
```http
GET /api/operation-logs/{id}
```

#### 根据用户ID查询
```http
GET /api/operation-logs/user/{userId}
```

#### 根据模块查询
```http
GET /api/operation-logs/module/{module}
```

支持模块: ASSET, LIABILITY, TRANSACTION

#### 根据操作类型查询
```http
GET /api/operation-logs/operation-type/{operationType}
```

支持类型: CREATE, UPDATE, DELETE

#### 根据时间范围查询
```http
GET /api/operation-logs/time-range?startTime=2026-01-01T00:00:00&endTime=2026-01-31T23:59:59
```

#### 复合查询
```http
POST /api/operation-logs/query
Content-Type: application/json

{
  "userId": 1,
  "module": "ASSET",
  "operationType": "CREATE",
  "startTime": "2026-01-01T00:00:00",
  "endTime": "2026-01-31T23:59:59",
  "limit": 100
}
```

## 操作日志字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键ID |
| userId | Long | 操作人ID |
| username | String | 操作人用户名 |
| realName | String | 操作人真实姓名 |
| operationType | String | 操作类型: CREATE, UPDATE, DELETE |
| module | String | 模块: ASSET, LIABILITY, TRANSACTION |
| operationName | String | 操作名称 |
| description | String | 操作描述 |
| targetId | Long | 目标记录ID |
| targetName | String | 目标记录名称 |
| requestData | String | 请求数据(JSON格式) |
| resultData | String | 结果数据(JSON格式) |
| ip | String | 操作IP地址 |
| status | String | 状态: SUCCESS, FAILED |
| errorMsg | String | 错误信息 |
| operationTime | LocalDateTime | 操作时间 |

## 已集成的操作日志

以下Service方法已自动集成操作日志:

### AssetService
- createAsset: 创建资产
- updateAsset: 更新资产
- deleteAsset: 删除资产

### LiabilityService
- createLiability: 创建负债
- updateLiability: 更新负债
- deleteLiability: 删除负债

### TransactionService
- create: 创建交易记录
- update: 更新交易记录
- delete: 删除交易记录

## 注意事项

1. **权限控制**: 所有操作日志查询接口都需要`LEADER`角色权限
2. **自动记录**: 使用`@OperationLog`注解的方法会自动记录操作日志
3. **操作人信息**: 系统自动从SecurityContext中获取当前登录用户信息
4. **数据完整性**: 所有创建和更新操作都会记录操作人ID

## 示例查询

### 查看某个用户的所有操作
```bash
curl -H "Authorization: Bearer <token>" \
  http://localhost:8080/api/operation-logs/user/1
```

### 查看资产模块的所有操作
```bash
curl -H "Authorization: Bearer <token>" \
  http://localhost:8080/api/operation-logs/module/ASSET
```

### 复合查询示例
```bash
curl -X POST -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"userId": 1, "module": "ASSET", "operationType": "CREATE"}' \
  http://localhost:8080/api/operation-logs/query
```
